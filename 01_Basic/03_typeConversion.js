// decleare a variables 
let score = "33";
// print the data present inside the variables. 
console.log(score);
// find datatype fo the score Variables. 
console.log(typeof(score));

// conversion of the data String to Number 
let numberScore = Number(score);
//let check the number is conver or not in side the number.
console.log(typeof(numberScore));

console.log("try to convert mix String in number ");

// try to convert mix String in number 

let mix = "22eer";
console.log(mix);
let numberMix = Number(mix);
//when you wan't convert the MixString to number its convert. but we wan't to print the output in console we get the NaN (Not A Number )    
console.log(typeof(numberMix));
console.log(numberMix);


console.log("conver the Boolean values in to number ");

let num = true ;
console.log(num);
let boolean1 = Number(num);
console.log(boolean1); 


